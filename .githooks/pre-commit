#!/usr/bin/env bash
set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT"
export PATH="$ROOT/node_modules/.bin:$ROOT/.tools:$PATH"

# Typecheck, tests and build run in CI (.github/workflows/ci.yml).
echo "▶ pre-commit: bun run precommit"
bun run precommit
