#!/usr/bin/env bash
set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT"
export PATH="$ROOT/node_modules/.bin:$ROOT/.tools:$PATH"

echo "▶ pre-commit: bun run check"
bun run check

echo "▶ pre-commit: bun run test"
bun run test
