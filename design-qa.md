# 模板编辑方案 2 — Design QA

Final result: **passed** (2026-09-10).

## 对比依据与环境

- 目标：用户选定的方案 2，生成稿 `exec-ecbbb60e-ddd3-4f1f-acd8-22081ce92764.png`。
- 实现：隔离临时 HOME、模拟服务商目录，浏览器 `http://127.0.0.1:8789/`。
- 将目标图与桌面实现截图放在同一次视觉输入中检查。桌面截图：`/tmp/hsw-template-desktop.png`；窄屏截图：`/tmp/hsw-template-mobile.png`。
- 检查尺寸：1440 × 1024、820 × 1180、390 × 844。未修改真实工具配置。

## 必查表面

| 表面 | 检查结果 |
| --- | --- |
| 字体与排版 | 使用产品既有字体和字号层级；模型 ID 用等宽字体且允许换行。标签、辅助文案、默认项和备注层级清楚。 |
| 间距与布局 | 桌面左右双栏，后续连接默认折叠；默认项及备注可见，保存操作固定在底部。平板和手机转单栏，内容可滚动。 |
| 颜色与表面 | 沿用白色、淡紫色与紫色主操作；使用既有边框、圆角、禁用态和选中态。 |
| 图片与资产 | 目标无照片或插画，不需要位图替代；保留产品原有图标资产。 |
| 图标 | 使用同一 Lucide 图标体系；连接、折叠、删除、参数设置、下拉图标位置一致，图标按钮有可访问名称。 |
| 文案与行为 | 多选、工具适配、默认模型、备注及暂存/应用区别明确。中英文目录同步。 |
| 可访问性 | 复用语义 checkbox、radio、select、tabs 与 dialog；标签及焦点可见。窄屏可访问下部工具设置与保存按钮，无观察到的横向重叠。 |

## 迭代发现

- P1（已修复）：初版连接卡过高，桌面首屏看不到默认项和备注。压缩已选模型行、合并标题区域，并默认折叠第二个及后续连接后复查通过。
- P2（已修复）：原集合入口与模板配置分散。入口集中到模板；Kimi/DSH 明确区分保存待应用配置与预览应用。
- P2（已修复）：Codex 推理设置文案不够直接，统一为“推理强度”，并依据模型已声明能力列出选项。
- 无剩余 P0/P1/P2。与生成稿的有意差异：采用产品既有弹窗外框、字号和页签；保留协议/地址编辑入口；Kimi 与 DSH 各自保存适配规则。

## 验证证据与边界

- 浏览器确认模板保存成功，重新打开后 Codex 默认模型及 `high` 推理强度保留；控制台错误为 0。
- Web 276 项、服务端 431 项测试通过，覆盖连接分组批量选择、配置分组、远端目录刷新与选择保留、多选、默认项移除保护、Claude 档位复用、引用重映射、取消流程、忽略修订及 Kimi/DSH 原生多模型应用。
- 模型下拉框下方显示目录状态及刷新操作；隔离浏览器验证刷新后下拉选项正常，原有三个已选模型保留。
- Codex 0.153.4 真实 app-server 成功读取生成的两个模型目录项；没有发送真实模型请求。
- 深色主题、辅助技术逐项朗读和所有浏览器组合未在本轮穷举验证。

## 用户截图问题复查

- Codex 无选项：现有 Anthropic 连接被协议筛选排除。改为明确提示及新建独立 Responses 连接入口，保留原连接；组件测试验证创建后可选且可保存。
- DSH 内容截断：弹窗内容区域使用明确高度与 `min-h-0`，模型摘要默认收起，预览操作固定底部。隔离浏览器展开文件差异并滚动验证底部始终可操作。
- 链路图被裁剪：移除 quick 预览的 580px 限制，为链路面板设置最小高度，外层允许滚动。桌面截图确认当前/新链路及工具节点完整，390 × 844 下取消和保存可见。
- 空工具选择及取消返回：改为工具页签，取消直接关闭流程，待更新入口保留原配置选择。
- 无法取消待更新：添加“忽略本次更新”及效果说明；服务端测试验证模型和关联保留、过期修订被拒绝、后续修订恢复提示。

## 真实服务复查：分组与收束

在 `http://localhost:8787/` 检查已有数据，没有点击保存或应用：DSH 的模板生成条目显示为一张“多模型配置 · 11 个模型”卡，独立配置及官方入口保持单独展示。模板关联摘要按配置组计数。

真实模板工具适配显示两个连接组，各含 11 个模型；组标题显示已选数量，展开后可见全选、清空和反选，模型区限制高度并可滚动。参数页从 22 个平铺条目收束为两个连接组，展开后以模型 ID 区分。批量操作使用隔离组件测试验证，未修改真实模板选择。

此前 `127.0.0.1:8789` 是模拟目录的隔离测试服务，其固定五个模型不能作为真实远端目录验证。本节观察来自实际 `localhost:8787` 页面。
