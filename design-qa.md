# 组合工作台设计验收

日期：2026-09-05。范围：工作台、模型关系视图、全局恢复时间线。用户选择组合三张概念稿；不是对其中一张进行逐像素复刻。

## 视觉依据与浏览器证据

- 概念稿目录：`/Users/seavey/.codex/generated_images/01a07016-1d1a-73c0-bc6f-0e8aad6992b8/`。
- 关系工作台：`exec-bca0d099-e905-4101-9664-66421cf01ae7.png`。
- 工具工作台：`exec-afa9594c-b5c6-4c86-8c68-d7d117fb6818.png`。
- 时间回廊：`exec-400ec9a3-29f1-48e7-a4fe-e53985561425.png`。
- 浏览器实现：`http://localhost:8787/`，Chrome，已登录用户的实际配置；本轮仅预览，没有执行实际激活或恢复。
- 已保存的实现截图：`/tmp/hsw-redesign/qa/timeline-before.jpg`。这是短窗口修正前的截图，不能作为修正后的通过证据。
- 实现截图为 1501 × 800 像素；概念图为 1487 × 1058 像素。浏览器返回截图的设备密度没有独立测量。二者在同一比较输入中打开，按整体层级进行比较，没有拉伸图片或声称同视口像素匹配。
- 工作台浅色、工作台深色、收藏关系页深色、恢复点深色、Claude 单工具预览均通过 CUA 捕获并在会话内查看。最后断连后未能保存其余状态的独立截图。

## 检查结果与迭代

### P2：短窗口中的恢复确认按钮在首屏之外

位置：`recovery-timeline/index.tsx`、`index.css`。

证据：修正前截图中，时间列表与文件影响均参与整个页面滚动，选较早恢复点时右侧预览可能留在页面上方；确认按钮被内容推到 800px 窗口下方。

已修改：桌面时间点列表限制高度并独立滚动；快照使用有高度约束的 flex 容器，文件影响区域独立滚动，快照标题、全局恢复范围和操作按钮不随文件列表滚动。窄屏保留顺序布局。

状态：等待修正后的同状态浏览器截图，不能仅凭 CSS 或构建通过关闭该问题。

### P2：关系图三列标题不在同一水平位置

证据：会话中的深色收藏页截图，供应商和模型列的标题随节点垂直居中，而工具列标题位于顶部。

已修改：三列伸展，标题在顶部，供应商和模型节点在余下空间居中。状态：等待修正后截图。

## 五项视觉检查

- 字体：复用 IBM Plex Sans、IBM Plex Mono 和中文系统回退。标题、模型 ID、说明层次清晰；长模型 ID 可换行。最终窄屏换行还需实机复查。
- 间距：保留工具列表/右侧操作区的主要比例，关系页采用供应商→模型→工具顺序；时间线具有实际恢复点的纵深层次。上列两个布局问题已修改，尚未完成复查。
- 颜色：复用现有 light/dark tokens；紫色代表选中关系，状态同时有文字。没有增加独立配色系统或用持续粒子暗示请求流量。
- 图像与图标：复用 BrandMark、现有 LobeHub 工具品牌图标和 Lucide 操作图标。未照搬生成稿中错误的工具图标和模型归类。图形节点和快照是交互组件，非栅格装饰占位。
- 文案：中英目录键一致；写入需明确确认，全局恢复范围明确。没有采用生成稿中“当前状态不会被覆盖”的误导文案，也没有推测备份内的历史模型拓扑。恢复 API 保持只返回影响元数据。

## 行为与自动化验证

- 实机点击工作台“预览切换”直接进入单工具预览；工具、渠道和激活模式沿用当前选择。Pierre 原生文件差异可见，没有观察到 ChunkLoadError 或运行时错误遮罩。
- 实机切换到收藏关系视图，五种工具及兼容性状态来自服务端；查看恢复点仅发出只读预览。
- Web 回归覆盖：单工具直接预览及确认后应用、多渠道明确选择、首次收藏预填且凭据提取默认不勾选、恢复点快速切换的乱序响应、离开页面后旧切换计划不回填、预览失败禁用恢复。
- 最新基线：服务端 395 通过；Web 193 通过；typecheck、lint、Biome、生产构建通过。保留原有 i18next lint 警告与两个构建体积警告。
- 减少动态效果通过 CSS 分支关闭新增位移和纵深动画；尚未实机切换系统偏好。浏览器控制台没有在断连后单独复查。

## 阻塞与剩余步骤

CUA 在最终复查期间报 `Sky Computer Use native pipe startup failed`；重新获取浏览器后 `listBrowsers()` 返回空数组，原 Chrome tab 与按名称打开 Chrome 均不可用。已请求重新连接 Chrome。没有改用未经授权的浏览器控制方式绕过该问题。

1. 恢复 Chrome 连接，回到相同恢复点，确认短窗口下标题、范围和确认按钮可见。
2. 重拍收藏关系页，检查三列标题与工具状态；补存工作台明暗主题截图。
3. 补做窄屏、键盘、减少动态效果和控制台检查，修正任何剩余 P0/P1/P2 问题。

final result: blocked
